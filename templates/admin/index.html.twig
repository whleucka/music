{% extends "layout/base.html.twig" %}

{% block title %}{{ parent() }} | Admin > {{ module.title }}{% endblock %}

{% block content %}
  <h1>{{ module.title }}</h1>
  {{ content|raw }}
{% endblock %}
